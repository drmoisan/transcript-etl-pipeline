# 2025-12-04-ci-coverage-gate — Spec

- **Issue:** [#28](https://github.com/drmoisan/transcript-etl-pipeline/issues/28)
- **Parent (optional):** [#21](https://github.com/drmoisan/transcript-etl-pipeline/issues/21)
- **Owner:** drmoisan
- **Last Updated:** 2026-02-02T11-43
- **Status:** Draft
- **Version:** 0.1

## Overview

CI does not currently enforce or surface coverage outcomes, so coverage can regress unnoticed. Add a CI coverage gate in `.github/workflows/ci.yml` that uses the configured `fail_under` threshold from `pyproject.toml` and publishes coverage outputs for each run.


## Behavior

Add coverage reporting in CI and enforce a configurable minimum coverage threshold sourced from `pyproject.toml` (initial floor aligned to current baseline). The CI quality-checks job runs Pytest with coverage, uploads HTML/XML artifacts, and publishes a GitHub Actions step summary. The job fails when total coverage is below the configured floor, with a documented ratchet plan to raise the floor over time.

## Coverage Gate Sequencing Criteria

Coverage ratchets align with milestone completion and module targets:

1. **Baseline gate (15%)** — current floor while #21–#23 are in progress.
2. **Gate to 20%** — after #21–#23 meet per-module targets and M1 is complete.
3. **Gate to 30%** — after #26–#27 meet coverage targets and M2 is complete.
4. **Gate to 50%** — after #24–#25 stabilize and M3 is complete.

Each ratchet requires updated coverage evidence in plan/spec files and an initiative milestone status update.


## Inputs / Outputs

- Inputs (CLI flags, files, env vars)
	- CI workflow definition in `.github/workflows/ci.yml`.
	- Pytest-cov invocation in CI: `poetry run pytest --cov=src/transcript_etl_pipeline --cov-report=term --cov-report=xml --cov-report=html`.
	- Coverage threshold in `pyproject.toml` under `[tool.coverage.report] fail_under = 15` (initial floor; ratchet plan below).
- Outputs (artifacts, logs, telemetry)
	- Coverage summary in CI logs.
	- GitHub Actions step summary with coverage table by file.
	- Coverage artifacts attached to the CI run (HTML report and `coverage.xml`).
- Config keys and defaults:
	- Coverage fail-under threshold default: 15% (ratchet upward over time).
- Versioning or backward-compatibility constraints:
	- Threshold increases must be coordinated with coverage improvements to avoid blocking merges.

## API / CLI Surface

Commands, flags, and examples:
- Example invocation used in CI:
	- `poetry run pytest --cov=src/transcript_etl_pipeline --cov-report=term --cov-report=xml --cov-report=html`
	- Threshold enforced via `pyproject.toml` (`[tool.coverage.report] fail_under = 15`).
	- Expected: CI logs include coverage summary; job fails if total coverage < `fail_under`.
- Contracts and validation rules:
	- CI must fail the job when coverage is below the configured floor.
	- CI must surface coverage results in both logs and the GitHub Actions step summary.

## Data & State

Data flow, storage, or state changes introduced by this feature.
- Data transformations and invariants:
	- Coverage data generated by pytest-cov remains accurate and reproducible per run.
- Caching or persistence details:
	- Coverage artifacts stored with the CI run only (no new persistent storage).
- Migration or backfill requirements (if any):
	- None; introduces a new gate and reporting only.

## Constraints & Risks

- Start with a conservative coverage floor aligned to baseline (~16%) to avoid blocking progress.
- Threshold should be easy to ratchet upward over time via `pyproject.toml`.
- Reporting should be CI-visible (logs, step summary, artifacts) without adding excessive runtime across the Python matrix.


## Implementation Strategy

- Implementation scope (what changes, not sequencing):
	- Update `.github/workflows/ci.yml` to run pytest-cov, publish a coverage summary, and upload HTML/XML artifacts.
	- Configure coverage floor in `pyproject.toml` under `[tool.coverage.report]`.
- New classes/functions/commands to add or update:
	- CI command update only; no new runtime code.
- Dependency changes (new/removed packages) and rationale:
	- None (pytest-cov already in use for coverage reporting).
- Logging/telemetry additions and locations:
	- CI logs include the coverage summary; upload HTML and XML artifacts.
- Rollout plan (feature flags, staged deploys, fallback path):
	- Start at 15% and raise incrementally as coverage improves; adjust `pyproject.toml`.

## Definition of Done

- [ ] Acceptance criteria documented and mapped to tests or demos (evidence: links to CI runs showing pass/fail at floor)
- [ ] Behavior matches acceptance criteria in all documented environments (evidence: CI run with coverage summary + artifacts)
- [ ] Tests updated/added (unit/integration as applicable) (evidence: pytest output in CI)
- [ ] Edge cases and error handling covered by tests (evidence: CI failure when `fail_under` > current coverage)
- [ ] Docs updated (README, docs/features/active/... links) (evidence: commit diffs)
- [ ] Telemetry/logging added or updated (if applicable) (evidence: GitHub Actions step summary)
- [ ] Toolchain pass completed (format → lint → type-check → test) (evidence: CI logs)

## Seeded Test Conditions (from potential)
- [ ] Verify CI coverage command runs and produces a report.
- [ ] Confirm CI fails when coverage is below the configured threshold.
- [ ] Confirm CI passes when coverage meets/exceeds the threshold.
