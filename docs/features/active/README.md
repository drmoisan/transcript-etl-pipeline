# Active Features

Place in-progress feature folders here using the template in `docs/features/templates/feature/`. Each folder should map to a GitHub issue and contain `user-story.md`, `spec.md`, and `plan.md`.
