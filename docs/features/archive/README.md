# Archived Features

After a feature is merged, move its folder here using the naming pattern `YYYY-MM-DD-<feature-name>/`. Keep the original planning files intact so future work can reference decisions and outcomes.
